import React from 'react';

export default class SpeedLimit extends React.Component {

  handleChange = e => {
    //execute the function sent by the ancestor/parent
  };

  render() {
    //Allows the user to enter the speed limit in kph
    
  }
}
