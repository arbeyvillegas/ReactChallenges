import React from 'react';

export default class SpeedInput extends React.Component {

  handleChange = e => {
    //Execute the function sent by the parent/ancestor
  };

  render() {
    //Allows the user to set the speed and shows the right label based on the measurement sent by the parent
    
  }
}
